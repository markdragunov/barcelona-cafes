# TLS sites when DOMAIN is set (reference; live config is generated by caddy-entrypoint.sh).
{$DOMAIN} {
	encode gzip
	reverse_proxy app:3847
}

{$ADMIN_DOMAIN} {
	encode gzip
	@root path /
	redir @root /admin 302
	reverse_proxy app:3847
}
